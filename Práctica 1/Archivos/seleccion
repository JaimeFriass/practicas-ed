void Ordenacion_Selección(string v[] ){

string temp;
int i,j,pos_min;

  for(i=0;i<v.size();i++){
	pos_min=i;
	for(j=i+1;j<v.size();j++)º
		if(v[j]<v[pos_min])
			pos_min=j;
	//Coloca el mínimo en v[i]
	tmp=v[i];
	v[i]=v[pos_min];
	v[pos_min]=tmp;
	}
}
